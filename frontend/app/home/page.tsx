"use client"

import { LandingSearch } from "@/components/LandingSearch"

const Home = () => {
  // Always show landing search page (with sidebar for authenticated users)
  return <LandingSearch />
}

export default Home
